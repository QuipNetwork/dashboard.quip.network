/node_modules
/dist
/tsconfig.tsbuildinfo
/bun.lock
/build

# env config
.env
.env.*
!.env.example

# Local Netlify folder
.netlify

# Superpowers brainstorming companion
.superpowers/

# Local SQLite runtime data — never committed; rebuilt by the indexer.
/data/

# Stray deno lockfile from tooling experiments (this is a Bun project).
/deno.lock

# Vendored sibling project kept locally as an architecture reference only.
# Never tracked, and excluded from `bun test` discovery.
/reference-material/
